{% extends "base.j2" %}

{% block main %}
    <link rel="stylesheet" href="/static/sty/plugin_details.css" />
    <div class="box-h flex-center-v">
    <div class="center-v">
        {% set start_button_message = "Start / Stop" %}
        {% if plugin.status == Status.STARTED %}
            🟢
            {% set start_button_message = "⏹ Stop" %}
        {% elif plugin.status == Status.STOPPED  %}
            🔴
            {% set start_button_message = "▶ Start" %}
        {% elif plugin.status == Status.ERROR  %}
            ⚠️
            {% set start_button_message = "Restart" %}
        {% else %}
            ⚪
            {% set start_button_message = "▶ Start" %}
        {% endif %}
    </div>
    <h2>{{ plugin.name }}</h2>
    <div class="spacer-flex-h"></div>
    <button>{{ start_button_message }}</button>
    <button><b> ... </b></button>
    </div>
    <p>{{ plugin.description }}</p>
    <div class="box-h navbar">
        <div class="spacer-flex-h"></div>
        <a class="button" href="/plugins/{{ plugin.plugin_id }}/plugin_view">View</a>
        <a class="button" href="/plugins/{{ plugin.plugin_id }}/plugin_integrations">Integrations</a>
        <a class="button" href="/plugins/{{ plugin.plugin_id }}/plugin_settings">Settings</a>
        <a class="button" href="/plugins/{{ plugin.plugin_id }}/plugin_logs">Logs</a>
    </div>
    <div id="plugin-details-main">
        {% block inner_content %}
            <p><i>Plugin has no view.</i></p>
        {% endblock %}
    </div>
{% endblock %}